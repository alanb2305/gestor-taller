Flask>=3.0
reportlab>=4.0
waitress>=3.0

# Nota: cuando todo funcione, conviene fijar las versiones EXACTAS con:
#   pip freeze > requirements.txt
# así el tribunal instala justo las mismas que tú.
#
# Bootstrap, Bootstrap Icons y Chart.js NO van aquí: son librerías de
# JavaScript/CSS y se cargan desde el HTML (CDN).
