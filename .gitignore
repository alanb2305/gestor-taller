# Python
__pycache__/
*.pyc

# PyInstaller (lo que se genera al construir el .exe; el .spec sí se sube)
build/
dist/

# Entornos virtuales
venv/
.venv/

# Base de datos generada (no se sube al repositorio)
datos/*.db

# Fotos de los daños subidas (tampoco se suben al repositorio)
datos/fotos/
